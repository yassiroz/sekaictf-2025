service challenge
{
    disable     = no
    type        = UNLISTED
    socket_type = stream
    protocol    = tcp
    port        = 9999
    wait        = no
    user        = sage
    server      = /usr/bin/sage
    server_args = /app/chall.sage
}
